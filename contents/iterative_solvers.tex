\section{Iterative Solvers}
% Diagonal
\begin{equation}
	\text{diag}(S) = \begin{cases}
		\{(\bm{0}, b) \} & \text{if} \; (\bm 0, b) \in S \\
		\emptyset & \text{else}
	\end{cases}
	\label{eq:stencil-diag}
\end{equation}